import React from "react";
import { Text } from "react-native";
import { Container } from "./styles";

const AppConfig = () => {
  return (
    <Container>
      <Text style={{ color: "white" }}>App config</Text>
    </Container>
  );
};

export default AppConfig;
